# RAG-langchain

Boilerplate for Implementation 2 of the RAG Framework Familiarisation Sprint.

Architecture:

```text
Documents -> Text Splitter -> Embeddings -> FAISS -> Retriever -> Prompt -> LLM
```

Implementation work is intentionally left as `NotImplementedError` markers in `src/`.
